{
  "schema": "http://json-schema.org/draft-04/schema",
  "title": "Bad Echo sprite sheet JSON schema",
  "type": "object",
  "properties": {
    "texturePath": {
      "type": "string",
      "description": "The path to the file containing the texture of the individual animation frames that compose the sprite sheet."
    },
    "rows": {
      "type": "integer",
      "description": "The number of rows of frames in the sprite sheet."
    },
    "columns": {
      "type": "integer",
      "description": "The number of columns of frames in the sprite sheet."
    },
    "rowUp": {
      "type": "integer",
      "description": "The row for upward movement."
    },
    "rowDown": {
      "type": "integer",
      "description": "The row for downward movement."
    },
    "rowLeft": {
      "type": "integer",
      "description": "The row for leftward movement."
    },
    "rowRight": {
      "type": "integer",
      "description": "The row for rightward movement."
    },
    "rowInitial": {
      "type": "integer",
      "description": "The row containing initially drawn frames, prior to any movement occurring."
    }
  },
  "required": ["texturePath", "rows", "columns", "rowUp", "rowDown", "rowLeft", "rowRight"],
  "additionalProperties": false
}
